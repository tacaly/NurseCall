const NavBar () => {
    return (
        <nav className="NavBar">
            <div className="links">
                <a href="/">Home</a>
            </div>
        </nav>
    );
}

export default NavBar;